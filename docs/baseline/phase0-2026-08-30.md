# Baseline — Phase 0 stabilization (2026-08-30)

Branch `chore/phase0-stabilize-baseline`, tip `96a8aa9`.

## Gate state

| Check | Result |
|---|---|
| `yarn typecheck` | clean |
| `yarn lint` | 0 errors, 33 warnings (raw-hex / react-refresh — tolerated) |
| `yarn test` | 476 pass / 478 — **2 known failures, allow-listed below** |

## Update — Phase 1 complete (commit `b2b70e9`)

The closing-section rebuild landed. The 2 allow-listed failures below are now
**green** — the gate is fully clean (typecheck, lint, **478/478 tests**). The
allow-list is empty.

---

## Allow-listed test failures (historical — now resolved)

Both in `tests/motion/closing-lattice-pinned-scroll.test.tsx`, owned by the
**closing-section rebuild** (`docs/closing-section-rebuild/handoff.md`). They
encode an intent that was never implemented:

1. *"renders ClosingLatticeSection with HeroCanvas in closure mode and headline"*
   — `getByText("CAPABILITY // PLATFORM")` fails; `MiniEstablishingShot` in
   `ClosingShelf.tsx` is called with no `category` prop.
2. *"reduced motion: headline is hidden and CTA card is immediately active"* —
   the `aria-hidden` headline wrapper has `display:none` but not `opacity:0`.

To be fixed (test + code) as part of Phase 1 of
`.claude/plans/use-a-simple-frontend-misty-tide.md`.

## What Phase 0 changed

`main` HEAD (`46faff1`) was shipping red — 65 `tsc` errors + 2 eslint errors in
three files untouched by recent feature work, plus stale-copy test drift. The
uncommitted WIP was verified to introduce **zero** new failures (stash + re-gate:
identical failure set).

- `e86af5e` eslint ignores `.claude/**` (was contributing ~495 lint errors)
- `2f680ae` typecheck/lint repair: `ProcessDiagram`, `HeroNodeNetwork`, `HeroGallery`
- `9fbb84c` `global-markets` anchor↔section id collision fixed; stale test matchers refreshed
- `657319b`..`96a8aa9` the WIP landed as 6 reviewable commits (chapter rail 8→6,
  consent store, ServiceGlobe colour fix, entrance tuning, preloader warm-up
  deferral, infra/docs)
